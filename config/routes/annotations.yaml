controllers:
  resource: ../../src/Controller/
  type: annotation
  prefix: /{_locale}
  requirements:
    _locale: '%app_locales%'
  defaults:
    _locale: '%locale%'

rest_controller:
  resource: ../../src/Controller/RestController.php
  type: annotation
  prefix: /api
